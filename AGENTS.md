# skills

<!-- AGENTS.md - Universal AI Agent Instructions -->

## Project Overview
This is a `skills` project, likely a repository containing multiple AI agent skill modules or tools. The project is organized into distinct skill directories, each representing a specific capability (e.g., code review, documentation generation). It is a content-focused repository, not a traditional application with source code, build tools, or a defined programming language/framework.

## Directory Structure
```
skills/
├── code-review/
├── doc-gen/
├── explain/
├── refactor/
└── test-gen/
```

## File Patterns
The repository is structured as a collection of skill directories. Each top-level directory (`code-review/`, `doc-gen/`, etc.) represents a distinct skill module. The project contains content files (7 total) within these directories, but no conventional source code files were detected.

## Core Commands
No build system, package manager, test runner, or linter was detected. There are no standard `npm`, `yarn`, `go`, or `cargo` commands defined for this repository.

## Git Conventions
Git is used in this project. Specific commit message conventions, branch naming patterns, or common scopes were not provided. Follow any existing patterns visible in the repository's git history.

## Coding Standards
As this is a content repository organized into skill modules, coding standards for a specific language or framework are not applicable. Consistency in organizing content within each skill directory should be maintained.

## Safety Rules
1.  **Do not modify** the existing `AGENTS.md` file without explicit approval, as it contains agent instructions.
2.  **Do not delete or rename** the core skill directories (`code-review/`, `doc-gen/`, `explain/`, `refactor/`, `test-gen/`) as they define the project's structure.
3.  **Require confirmation** before creating new top-level directories outside the established skill set.
4.  **Require confirmation** before making sweeping changes to multiple content files across different skill directories.

## Permissions
**Allowed Without Prompt:**
*   Creating, editing, or deleting content files *within* an existing skill directory.
*   Updating documentation or non-critical text files.

**Requires Confirmation:**
*   Modifying the `AGENTS.md` file.
*   Creating a new top-level directory/skill module.
*   Renaming, deleting, or merging existing skill directories.
*   Changing the repository's root-level configuration or adding package/configuration files (e.g., `package.json`, `go.mod`).

**Never Allowed:**
*   Deleting the entire repository or its git history.
*   Removing the `.git` directory or critical version control files.
---

*Generated by Aii CLI /init command with LLM enhancement (~739 tokens, 49 lines)*